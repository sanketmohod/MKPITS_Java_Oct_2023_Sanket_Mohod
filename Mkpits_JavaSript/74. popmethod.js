// pop method - remove last elements from the array

let arr = [25, 35, 15, 45, 65, 75];
console.log(arr);
// pop method
console.log(arr.pop()); // it will remove the last welement and print it
console.log(arr); //  remove last element
