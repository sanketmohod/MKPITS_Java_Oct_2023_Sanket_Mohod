// comparison operator strict equals(===)
// Compares the equality of two operands with type. If both value and type are equal then the condition is true otherwise false.
let a = 18;
let b = 18;
console.log(a === b); // true
let str = "123";
let c = 123;
console.log(str === c); // false
