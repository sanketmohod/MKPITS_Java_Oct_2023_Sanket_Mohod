// slice method - it will out the elements from array
// return new array

let arr = [12, 15, 14, 18, 19, 13];
console.log(arr);
//let arr1 = arr.slice(3);// it will out all the elements from index 3
let arr1 = arr.slice(2, 4);// it will out element with index from 2 to 4 only

console.log(arr1);
