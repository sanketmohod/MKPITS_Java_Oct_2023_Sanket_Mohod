// initialization of variable
var number = 45;
var name = "rohit";
var isActive = false;
var age; // we can declare the  variable but not initialize it. It will be undefined until assigned a value.
console.log(age);
// var keyword - is a global variable we can access it globally throught the program.
// we can re-declare the same variable with updated value
var isActive = true;
console.log(number);
console.log(name);
console.log(isActive);
