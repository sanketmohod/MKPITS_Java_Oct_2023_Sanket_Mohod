function Power() {
  let x = parseInt(prompt("enter the value of x : "));
  document.write(x + " ");
  let n = parseInt(prompt("enter the value of n : "));
  document.write(n + " ");
  // let x = 5;
  // let n = 2;
  document.write(Math.pow(x, n));
}
