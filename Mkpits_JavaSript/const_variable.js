// initialization of variable
const numb = 18;
const name = "virat";
const isCricketer = true;
// const variable - is the block variable having accesesibility throught the block only.
// once we initialize the variable the value of variablr is fixed we can not change its value.
// we can not re-declare the variable
// const name = "vira";
console.log(numb);
console.log(name);
console.log(isCricketer);
