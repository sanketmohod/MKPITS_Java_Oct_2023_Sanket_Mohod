// bitwise not operator(~)
// This operator is used to invert the boolean value of the operand
// it will gives the nagation value incremeted by on eg. if a = 5 then ~a = -6

let a = 6; //110
let b = 2; //010
console.log(~b);
console.log(~(a & b)); // a & b -> 010 -> 2, ~(a & b) - > -3
