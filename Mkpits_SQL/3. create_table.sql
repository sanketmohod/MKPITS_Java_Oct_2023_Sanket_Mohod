use customer;
-- use - is the commond by which we can use the database
create table customer_data(id int);
-- create table - is use to create the table in database which required at least one column to create table
