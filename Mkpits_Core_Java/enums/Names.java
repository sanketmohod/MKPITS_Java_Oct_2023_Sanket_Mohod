package com.mkpits.enums;
// enum - is a special class the represent the group of constant(unchangeable variables)
// constant variable are separated by comma and use only uppercase letters
public enum Names {
      KARAN,
      RAJ,
      AASHIK,
      ROHIT
}
